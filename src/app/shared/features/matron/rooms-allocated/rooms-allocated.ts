import { Component } from '@angular/core';
import { Card } from "../../../components/card/card";

@Component({
  imports: [Card],
  selector: 'app-rooms-allocated',
  styleUrl: './rooms-allocated.css',
  templateUrl: './rooms-allocated.html',
})
export class RoomsAllocated {
}
